# Archived Research

This directory keeps historical comparison notes and diagrams only.

The source mirrors that originally backed some historical evidence paths were removed from the tracked repository during the Whiteboard Agent Alpha cleanup. A local archive was created at:

```text
.local-archives/vigilon-reference-mirrors-20260522-2132.tar.gz
```

Do not treat `sources/...`, `research/sources/...`, `packages/claude-code/...`, or `docs/claudecode-research/...` references inside older notes as live repository paths. They are archival references from the research phase.
