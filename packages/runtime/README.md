# @vigilon/runtime

Core runtime and CLI for Vigilon Agent.

```bash
pnpm add @vigilon/runtime
vigilon --help
```

The runtime expects a DeepSeek-compatible key for live model calls:

```bash
export DEEPSEEK_API_KEY=...
```

Use the repository root README for full development and release gates.
